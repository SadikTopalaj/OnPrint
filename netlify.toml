[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Stripe webhook trenger raw body - handteres direkte i function-koden
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
